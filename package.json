{
	"name": "damianocsl.github.io",
	"version": "0.0.1",
	"description": "Personal website",
	"main": "src/index.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "light-server --baseDir=\"dist\""
	},
	"author": "damianocsl",
	"license": "MIT",
	"devDependencies": {
		"light-server": "^2.9.1"
	}
}
